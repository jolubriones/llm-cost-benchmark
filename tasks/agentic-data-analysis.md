You are given a messy CSV (columns: Name, Date, Score; ~10k rows) with mixed date formats,
missing values, and duplicate rows. Write a Python function clean_data(df) using pandas that
returns a cleaned DataFrame. State your assumptions as code comments.
Return ONLY the function code.