Build a small single-file web app (HTML + CSS + JS in one file): a "Player Score Tracker".
Requirements:
- Input fields: player name, score, an Add button.
- Displays a table of players sorted by score (descending).
- No frameworks, no external libraries — vanilla JS only.
Return the complete file contents.